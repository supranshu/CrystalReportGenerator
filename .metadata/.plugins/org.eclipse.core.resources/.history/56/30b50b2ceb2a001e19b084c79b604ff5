<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
    <meta charset="UTF-8">
    <title>Generate Report</title>
    <!-- Link to Bootstrap CSS -->
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
</head>
<body>
    <div class="container mt-5">
        <h1>Generate Jasper Report</h1>
        <form th:action="@{/generate-report}" method="get" target="_blank">
            <div class="form-group">
                <label for="year">Year:</label>
                <input type="text" class="form-control" id="year" name="year" placeholder="Enter year (e.g., SE, FE, BE, TE)">
            </div>
            <div class="form-group">
                <label for="branch">Branch:</label>
                <input type="text" class="form-control" id="branch" name="branch" placeholder="Enter branch (e.g., ComputerScience)">
            </div>
            <button type="submit" class="btn btn-primary">Generate Report</button>
        </form>
    </div>

    <!-- Added the below table and buttons for exporting data -->

    <div class="container mt-5">
        <h2>Student Data Table</h2>
        <table class="table table-striped">
            <thead>
                <tr>
                    <th>Roll Number</th>
                    <th>Name</th>
                    <th>Year</th>
                    <th>Branch</th>
                    <th>Attendance</th>
                </tr>
            </thead>
            <tbody>
                <!-- Replace 'studentsData' with actual data fetched from the backend -->
                <!-- You can use Thymeleaf to iterate through the data and display it in the table -->
                <tr th:each="student : ${studentsData}">
                    <td th:text="${student.rollNumber}"></td>
                    <td th:text="${student.name}"></td>
                    <td th:text="${student.year}"></td>
                    <td th:text="${student.branch}"></td>
                    <td th:text="${student.attendance}"></td>
                </tr>
            </tbody>
        </table>

        <!-- Add buttons to export data in different formats -->
        <div class="mt-3">
            <a class="btn btn-secondary" th:href="@{/export/csv}" target="_blank">Export as CSV</a>
            <a class="btn btn-secondary" th:href="@{/export/excel}" target="_blank">Export as Excel</a>
            <!-- Add more buttons for other formats (e.g., text, Jasper) as needed -->
        </div>
    </div>

    <!-- Optional: Add a script tag to include Bootstrap's JavaScript -->
    <!-- Add this before the closing </body> tag -->
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.6/dist/umd/popper.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
